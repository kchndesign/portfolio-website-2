// when window loads, trigger tooltip to show user that there is a light mode option

window.onload = () => {
    const element = document.querySelector('.toggle__wrapper');

    console.log('triggering mouseenters');
    $('.toggle__wrapper');
};
